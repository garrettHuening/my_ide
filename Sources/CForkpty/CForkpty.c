// All implementation lives in the header (`static inline` wrappers).
// This file exists to give the target a translation unit so SwiftPM
// builds a library rather than a header-only target.
#include "CForkpty.h"
